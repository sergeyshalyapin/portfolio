const { generateRandomNumber, celciusToFahrenheit } = require('./utils');

console.log(`Random number: ${generateRandomNumber()}`);
console.log(`Fahrenheit from 0 celcius: ${celciusToFahrenheit(0)}`);
